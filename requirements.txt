# LipReader backend dependencies (open-vocabulary VSR engine).
# Python 3.10. CPU inference works fine — no GPU required.
# Install into a dedicated venv:  python3.10 -m venv .venv-vsr
#                                 .venv-vsr/bin/pip install -r requirements.txt

# --- Deep learning (Auto-AVSR / espnet, vendored under vsr/) ---
torch==2.12.1
torchvision==0.27.1
torchaudio==2.11.0
sentencepiece==0.2.1

# --- Face / mouth detection + video IO ---
# numpy pinned <2 for mediapipe 0.10.14 compatibility.
numpy==1.26.4
mediapipe==0.10.14
opencv-contrib-python==4.11.0.86
scipy==1.15.3
scikit-image==0.25.2
av==17.1.0
imageio==2.37.3
hydra-core==1.3.3

# --- API ---
fastapi==0.110.0
uvicorn[standard]==0.27.1
python-multipart==0.0.9
